import { test } from 'vitest';

test('Create a "TabChannel" instance.', () => {});
test('Use the "BroadcastChannel" nammed "x" to send data.', () => {});
test('Send data only to other tab.', () => {});
test('Send data only to current and other tab. (option self)', () => {});
test('Receive data only once from a listener. (option once)', () => {});
